# Claude Code 自动化开发工作流指令

> 版本：v1.0  
> 用途：新功能开发的完整自动化流程

---

## 🎯 工作流总览

当我请求添加新功能时，请严格按照以下 7 个阶段执行，每个阶段完成后自动运行检查点。

---

## 📋 阶段 1：准备与分析

### 任务清单
- [ ] 检查项目环境（Node.js 版本、数据库连接）
- [ ] 扫描现有代码结构（确认架构模式）
- [ ] 查找类似功能（避免重复开发）
- [ ] 检查未提交的变更（`git status`）
- [ ] 分析需求并确认理解

### 自动化检查点
```bash
# 环境检查
node --version
npm --version

# Git 状态检查
git status
git diff --stat

# 数据库连接检查
npm run db:check || echo "请确保数据库连接正常"
```

### 输出
- 现有代码结构分析报告
- 相关模块列表
- 潜在冲突警告

---

## 📋 阶段 2：数据库设计

### 任务清单
- [ ] 设计表结构（字段、类型、约束）
- [ ] 检查与现有表的关系（外键）
- [ ] 设计索引策略（主键、外键、常用查询字段）
- [ ] 验证字段命名一致性（遵循项目规范）
- [ ] 生成 migration 脚本（up/down）

### 关键检查项
```
✓ 是否所有外键都有对应索引？
✓ 时间字段是否统一使用 created_at/updated_at？
✓ 是否需要软删除（deleted_at）？
✓ 枚举类型是否使用 ENUM 或 VARCHAR？
✓ 是否添加了必要的唯一索引？
```

### 自动化检查点
```bash
# 验证 migration 语法
npm run migration:validate

# 检查表结构冲突
npm run db:schema:check
```

### 输出
- 完整的 migration 文件
- 数据库变更说明文档
- ER 图（如果涉及多表）

---

## 📋 阶段 3：代码实现

### 任务清单
- [ ] 创建/更新 Model/Entity 层
- [ ] 实现 Service/业务逻辑层
- [ ] 实现 Controller/API 层
- [ ] 添加输入验证（DTO/Schema validation）
- [ ] 实现错误处理和异常捕获
- [ ] 添加事务处理（如涉及多表操作）
- [ ] 添加日志记录点（关键操作）

### 代码规范检查
```
✓ 是否遵循项目现有架构模式？
✓ 变量命名是否符合规范（camelCase/PascalCase）？
✓ 是否添加了 TypeScript 类型定义？
✓ 是否有足够的注释（复杂逻辑）？
✓ 错误消息是否用户友好？
```

### 自动化检查点
```bash
# 代码风格检查
npm run lint

# TypeScript 类型检查
npm run type-check

# 代码格式化
npm run format

# 构建检查
npm run build
```

### 输出
- 完整的代码文件（Model/Service/Controller）
- 错误码定义文件（如适用）

---

## 📋 阶段 4：数据库迁移与测试

### 任务清单
- [ ] 执行 migration（在开发环境）
- [ ] 验证表结构正确创建
- [ ] 插入测试数据（seed data）
- [ ] 验证外键约束
- [ ] 测试索引性能（EXPLAIN 查询计划）

### 自动化检查点
```bash
# 执行 migration
npm run migration:run

# 验证表结构
npm run db:schema:dump

# 插入测试数据
npm run db:seed

# 验证数据完整性
npm run db:validate
```

### 输出
- Migration 执行日志
- 数据库快照（当前结构）

---

## 📋 阶段 5：功能测试

### 任务清单
- [ ] 编写单元测试（Service 层）
- [ ] 编写集成测试（API 层）
- [ ] 测试正常流程（happy path）
- [ ] 测试边界条件（空值、极限值）
- [ ] 测试错误场景（非法输入、数据库错误）
- [ ] 测试并发场景（如涉及写操作）
- [ ] 性能基准测试（响应时间）

### 测试用例设计
```
正常测试：
  ✓ 创建记录（有效数据）
  ✓ 读取记录（存在的ID）
  ✓ 更新记录（部分字段）
  ✓ 删除记录

边界测试：
  ✓ 空字符串、null、undefined
  ✓ 超长字符串（varchar限制）
  ✓ 负数、零、极大值
  ✓ 不存在的ID（404）

错误测试：
  ✓ 缺少必填字段（400）
  ✓ 重复唯一字段（409）
  ✓ 外键约束违反（400）
  ✓ 数据库连接失败（500）
```

### 自动化检查点
```bash
# 运行所有测试
npm test

# 生成覆盖率报告
npm run test:coverage

# 运行性能测试
npm run test:performance

# 检查测试覆盖率阈值
npm run test:check-coverage
```

### 输出
- 测试文件（*.test.ts / *.spec.ts）
- 测试覆盖率报告
- 性能测试结果

---

## 📋 阶段 6：文档与安全

### 任务清单
- [ ] 生成/更新 API 文档（Swagger/OpenAPI）
- [ ] 更新数据库文档（数据字典）
- [ ] 更新 CHANGELOG.md
- [ ] 安全检查（SQL注入、XSS、权限）
- [ ] 敏感数据加密检查

### 安全检查清单
```
✓ 是否所有输入都经过验证？
✓ 敏感数据是否加密存储（密码、token）？
✓ API 是否需要鉴权？（JWT/Session）
✓ 是否防范 SQL 注入？（参数化查询）
✓ 是否有 CORS 配置？
✓ 是否有速率限制？（防止暴力攻击）
```

### 自动化检查点
```bash
# 安全漏洞扫描
npm audit

# 依赖检查
npm outdated

# 生成 API 文档
npm run docs:generate

# 验证 OpenAPI 规范
npm run openapi:validate
```

### 输出
- API 文档（Swagger JSON/YAML）
- CHANGELOG 条目
- 安全检查报告

---

## 📋 阶段 7：最终审查与交付

### 任务清单
- [ ] 代码质量总检查
- [ ] 性能审查（N+1 查询、全表扫描）
- [ ] 向后兼容性检查
- [ ] 准备部署清单
- [ ] 准备回滚方案
- [ ] 生成 Git commit message

### 最终检查清单
```
代码质量：
  ✓ Lint 无错误
  ✓ 类型检查通过
  ✓ 测试覆盖率 >= 80%
  ✓ 构建成功

性能：
  ✓ 无 N+1 查询问题
  ✓ 查询使用索引（EXPLAIN 分析）
  ✓ API 响应时间 < 500ms

文档：
  ✓ API 文档完整
  ✓ CHANGELOG 已更新
  ✓ README 已更新（如需要）

安全：
  ✓ 无安全漏洞（npm audit）
  ✓ 敏感数据已加密
  ✓ 鉴权机制正确
```

### 自动化检查点
```bash
# 完整检查流程
npm run lint && \
npm run type-check && \
npm run test && \
npm run build && \
npm audit && \
echo "✅ 所有检查通过！"

# Git 检查
git diff --check  # 检查空白符错误
git log --oneline -5  # 查看最近提交
```

### 输出
- 部署清单（deployment checklist）
- 回滚方案文档
- 规范的 Git commit message
- 功能交付报告

---

## 🔄 回滚方案模板

```markdown
### 回滚步骤

**数据库回滚：**
```bash
npm run migration:rollback
```

**代码回滚：**
```bash
git revert <commit-hash>
```

**验证回滚：**
- 检查数据库表结构
- 运行测试套件
- 验证 API 端点
```

---

## 📊 Git Commit Message 规范

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型：**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具

**示例：**
```
feat(user): 添加用户头像上传功能

- 新增 user_avatars 表
- 实现图片上传 API
- 添加文件大小和格式验证
- 完成单元测试和集成测试

Closes #123
```

---

## 🎯 使用说明

### 激活工作流
当我说以下任何内容时，请启动完整工作流：
- "添加新功能：[功能描述]"
- "实现：[功能描述]"
- "创建：[功能描述]"

### 阶段确认
- 每完成一个阶段，显示检查点结果
- 询问是否继续下一阶段
- 如有错误，提供详细的修复建议

### 灵活调整
- 如果某些阶段不适用，可以跳过
- 如果需要额外步骤，主动提出
- 始终以代码质量和安全为优先

---

## 🔧 配置建议

### package.json 脚本示例
```json
{
  "scripts": {
    "lint": "eslint . --ext .ts,.js",
    "type-check": "tsc --noEmit",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "build": "tsc",
    "migration:run": "npm run typeorm migration:run",
    "migration:rollback": "npm run typeorm migration:revert",
    "migration:validate": "npm run typeorm migration:show",
    "db:seed": "npm run typeorm migration:seed",
    "db:check": "npm run typeorm query \"SELECT 1\"",
    "docs:generate": "swagger-jsdoc -d swagger.config.js -o docs/openapi.json",
    "openapi:validate": "swagger-cli validate docs/openapi.json"
  }
}
```

---

## ⚠️ 重要提醒

1. **每个阶段都要执行自动化检查**
2. **所有测试必须通过才能进入下一阶段**
3. **敏感信息（密码、token）永远不要硬编码**
4. **数据库变更必须有回滚方案**
5. **代码提交前必须通过 lint 和 type-check**

---

---

## 🔗 Git 远程仓库管理

### 仓库配置

本项目配置了两个远程仓库：

| 远程名称 | 仓库地址 | 用途 |
|---------|---------|------|
| `origin` | `git@github.com:kieranchan/claude-relay-service.git` | 你自己的仓库（推送用） |
| `upstream` | `https://github.com/Wei-Shaw/claude-relay-service.git` | 原仓库（拉取更新用） |

### 常用命令

**推送到自己的仓库：**
```bash
git push origin main
```

**拉取原仓库更新：**
```bash
git fetch upstream
git merge upstream/main
```

**查看远程仓库配置：**
```bash
git remote -v
```

### 合并冲突处理策略

当原仓库更新与本地修改冲突时，有以下处理方式：

1. **逐个手动解决**（推荐）
   ```bash
   git fetch upstream
   git merge upstream/main
   # Git 会标记冲突文件，手动编辑解决后：
   git add <冲突文件>
   git commit
   ```

2. **优先保留本地修改**
   ```bash
   git merge upstream/main -X ours
   ```

3. **优先使用原仓库版本**
   ```bash
   git merge upstream/main -X theirs
   ```

4. **安全合并（先暂存本地修改）**
   ```bash
   git stash                    # 暂存当前修改
   git fetch upstream
   git merge upstream/main
   git stash pop                # 恢复本地修改
   ```

5. **对比特定文件差异后决定**
   ```bash
   git diff main upstream/main -- <文件路径>
   ```

### Claude Code 工作流提醒

> 当用户请求拉取原仓库更新时，使用：
> ```bash
> git fetch upstream && git merge upstream/main
> ```
> 如有冲突，询问用户选择哪种处理策略。

---

**创建者：** riyoyoyo
**最后更新：** 2025-12-26
**适用项目：** Node.js + TypeScript + TypeORM/Prisma